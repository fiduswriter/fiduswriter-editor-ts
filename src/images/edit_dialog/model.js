// TODO: image editing was originally part of the usermedia Django app.
// This stub keeps the editor package buildable until a decision is made on
// whether image editing belongs in @fiduswriter/editor, @fiduswriter/common,
// or a separate package.
export const imageEditModel = () => ({content: []})
